import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    padding: 10
  },
  contact: {
    marginBottom: 20
  },
  text: { fontSize: 20 },
  navButton: {
    fontSize: 24,
    fontWeight: 'bold'
  }
})
